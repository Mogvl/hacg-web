{
  "name": "hacg-web",
  "version": "1.5.6",
  "private": true,
  "description": "HACG (琉璃神社) web port — feature-parity reimplementation of https://github.com/yueeng/hacg (Apache-2.0)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "test": "npm run test -w backend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
