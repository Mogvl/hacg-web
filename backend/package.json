{
  "name": "hacg-web-backend",
  "version": "1.5.6",
  "private": true,
  "description": "HACG web backend: proxy + parsers (feature parity with yueeng/hacg Android app)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.21.2"
  }
}